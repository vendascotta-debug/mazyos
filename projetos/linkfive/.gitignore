node_modules
.next
*.tsbuildinfo
next-env.d.ts

# Segredos: nada de .env real no repositorio.
.env
.env.local
.env*.local

# ...mas o modelo sem segredo nenhum precisa ficar versionado.
!.env.example

# Estado local do CLI da Vercel (ids do projeto, token OIDC).
.vercel
